# Exercise 3: Even or Odd Checker
#
# Write a Python function called `is_even` that takes one integer argument
# and returns `True` if the number is even, and `False` otherwise.
#
# Example:
# is_even(4)   # Expected output: True
# is_even(7)   # Expected output: False
# is_even(0)   # Expected output: True
# is_even(-2)  # Expected output: True
#
# Guidance for Tests (`tests/test_exercise_3.py`):
# - Test with a positive even number.
# - Test with a positive odd number.
# - Test with zero.
# - Test with a negative even number.
# - Test with a negative odd number.


def is_even(number: int) -> bool:
    """Check if a number is even.

    Args:
        number: The number to check.

    Returns:
        bool: True if the number is even, False otherwise.
    """
    # Your code here
    pass
